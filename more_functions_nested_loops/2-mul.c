#include "main.h"
/**
 * mul - multiplies twwo integers
 * @a: first parameter
 * @b: second paramter
 * Return: the result
 */
int mul(int a, int b)
{
	int result;

	result =  a * b;
	return (result);
}
